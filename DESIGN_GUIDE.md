# TravelKZ - Полный дизайн-гайд

## 🎨 Обзор дизайн-системы

TravelKZ - это современное приложение для путешествий по Казахстану с премиальным UI/UX дизайном, основанным на принципах Glassmorphism, градиентов и микроанимаций.

### Философия дизайна

- **Минимализм с деталями** - чистый интерфейс с акцентом на контент
- **Максимальный фокус на фото** - изображения мест как главный элемент
- **Плавные анимации** - естественные переходы и микроинтерации
- **Современные эффекты** - Glassmorphism, градиенты, тени
- **Казахстанская айдентика** - цвета и элементы, отражающие культуру страны

## 🎨 Цветовая палитра

### Основные градиенты

```dart
// Основной градиент (фиолетово-синий)
static const Gradient primaryGradient = LinearGradient(
  colors: [Color(0xFF667EEA), Color(0xFF764BA2)],
);

// Акцентный градиент (розовый)
static const Gradient accentGradient = LinearGradient(
  colors: [Color(0xFFF093FB), Color(0xFFF5576C)],
);

// Вторичный градиент (голубой)
static const Gradient secondaryGradient = LinearGradient(
  colors: [Color(0xFF4FACFE), Color(0xFF00F2FE)],
);
```

### Темная тема

```dart
// Фоны
darkBackgroundPrimary: #0F0F1F
darkBackgroundSecondary: #1A1A2E
darkSurface: #16213E

// Текст
darkTextPrimary: #FFFFFF
darkTextSecondary: #B0B0B0
```

### Светлая тема

```dart
// Фоны
lightBackgroundPrimary: #FAFAFE
lightBackgroundSecondary: #F5F5FF
lightSurface: #FFFFFF

// Текст
lightTextPrimary: #1A1A2E
lightTextSecondary: #656E82
```

### Акцентные цвета

```dart
success: #10B981    // Зеленый для успеха
warning: #F59E0B    // Оранжевый для предупреждений
error: #EF4444      // Красный для ошибок
info: #3B82F6       // Синий для информации
```

## 📝 Типографика

### Семейства шрифтов

- **Основной**: Poppins (Google Fonts)
- **Вторичный**: Inter (Google Fonts)

### Стили заголовков

```dart
// H1 - Заголовки экранов
TextStyle h1 = TextStyle(
  fontFamily: 'Poppins',
  fontSize: 32,
  fontWeight: FontWeight.bold,
  letterSpacing: -0.5,
  height: 1.2,
);

// H2 - Подзаголовки
TextStyle h2 = TextStyle(
  fontFamily: 'Poppins',
  fontSize: 24,
  fontWeight: FontWeight.w600,
  height: 1.3,
);

// H3 - Заголовки секций
TextStyle h3 = TextStyle(
  fontFamily: 'Poppins',
  fontSize: 18,
  fontWeight: FontWeight.w600,
  height: 1.4,
);
```

### Стили текста

```dart
// Основной текст
TextStyle bodyLarge = TextStyle(
  fontFamily: 'Inter',
  fontSize: 16,
  fontWeight: FontWeight.w400,
  height: 1.5,
);

// Средний текст
TextStyle bodyMedium = TextStyle(
  fontFamily: 'Inter',
  fontSize: 14,
  fontWeight: FontWeight.w400,
  height: 1.4,
);

// Мелкий текст
TextStyle bodySmall = TextStyle(
  fontFamily: 'Inter',
  fontSize: 12,
  fontWeight: FontWeight.w400,
  height: 1.3,
);
```

## 📐 Размеры и отступы

### Система отступов (8dp grid)

```dart
spacing4: 4.0    // Минимальный отступ
spacing8: 8.0    // Базовый отступ
spacing12: 12.0  // Средний отступ
spacing16: 16.0  // Стандартный отступ
spacing20: 20.0  // Большой отступ
spacing24: 24.0  // Очень большой отступ
spacing32: 32.0  // Максимальный отступ
```

### Радиусы закругления

```dart
radiusSmall: 8.0     // Мелкие элементы
radiusMedium: 12.0   // Средние элементы
radiusLarge: 16.0    // Кнопки, поля ввода
radiusXLarge: 20.0   // Карточки
radiusXXLarge: 24.0  // Модальные окна
```

### Высота компонентов

```dart
buttonHeight: 56.0    // Высота кнопок
inputHeight: 52.0     // Высота полей ввода
cardHeight: 200.0     // Высота карточек
```

## 🌟 Компоненты UI

### 1. Градиентные кнопки

#### Основная кнопка
```dart
GradientButton(
  text: 'Добавить в маршрут',
  onPressed: () {},
  gradient: TravelKZDesign.primaryGradient,
  icon: Icons.add,
)
```

**Особенности:**
- Градиентный фон
- Микроанимация масштаба при нажатии (0.98x)
- Ripple эффект
- Haptic feedback

#### Вторичная кнопка
```dart
GradientButton(
  text: 'Отмена',
  type: ButtonType.secondary,
  onPressed: () {},
)
```

**Особенности:**
- Прозрачный фон с градиентной границей
- Анимация заполнения при нажатии

#### Кнопка-иконка
```dart
GradientIconButton(
  icon: Icons.favorite,
  onPressed: () {},
  gradient: TravelKZDesign.accentGradient,
)
```

### 2. Glassmorphic карточки

#### Карточка места
```dart
PlaceCard(
  imageUrl: 'https://example.com/image.jpg',
  title: 'Алматы',
  location: 'Южный Казахстан',
  rating: 4.8,
  reviewCount: 1250,
  isFavorite: true,
  onTap: () {},
  onFavoriteTap: () {},
)
```

**Особенности:**
- Glassmorphic эффект с размытием
- Градиентный оверлей на изображении
- Анимированная кнопка избранного
- Hover эффекты

#### Карточка маршрута
```dart
RouteCard(
  title: 'Путешествие по Алматы',
  dateRange: '15-20 мая',
  placesCount: 8,
  budget: 150000,
  progress: 0.6,
  onTap: () {},
)
```

### 3. Поля ввода

#### Градиентное поле
```dart
GradientInputField(
  hintText: 'Поиск мест...',
  prefixIcon: Icon(Icons.search),
  onChanged: (value) {},
)
```

**Особенности:**
- Градиентная граница при фокусе
- Плавная анимация фона
- Анимация масштаба

#### Поисковая строка
```dart
AnimatedSearchField(
  hintText: 'Поиск мест в Казахстане...',
  onSearchChanged: (query) {},
  leading: Icon(Icons.search),
)
```

### 4. Навигация

#### Анимированная нижняя навигация
```dart
AnimatedBottomNav(
  currentIndex: 0,
  items: TravelKZBottomNavItems.defaultItems,
  onTap: (index) {},
)
```

**Элементы навигации:**
- Главная (дом)
- Поиск (лупа)
- Маршруты (маршрут)
- Избранное (сердце)
- Профиль (человек)

**Особенности:**
- Glassmorphic фон
- Анимированный пузырек вокруг активного таба
- Плавное появление/исчезновение лейблов

#### AppBar с эффектами
```dart
AnimatedAppBar(
  title: 'TravelKZ',
  actions: [IconButton(...)],
)
```

**Типы AppBar:**
- Обычный с Glassmorphism
- Плавающий с градиентом
- С анимированным поиском

## 🎬 Анимации и микроинтерации

### 1. Анимация лайка
```dart
MicroInteractions.likeAnimation(
  isLiked: true,
  child: Icon(Icons.favorite),
  onTap: () {},
)
```

**Эффекты:**
- Взрыв частиц при лайке
- Масштабирование иконки
- Изменение цвета
- Haptic feedback

### 2. Добавление в маршрут
```dart
MicroInteractions.addToRouteAnimation(
  isAdded: true,
  child: Icon(Icons.add),
  onTap: () {},
)
```

**Эффекты:**
- Появление галочки
- Масштабирование
- Bounce анимация

### 3. Анимация табов
```dart
MicroInteractions.tabAnimation(
  isSelected: true,
  child: Icon(Icons.home),
  onTap: () {},
)
```

**Эффекты:**
- Пузырек вокруг активного таба
- Масштабирование иконки
- Градиентный фон

### 4. Анимация карточек
```dart
MicroInteractions.cardTapAnimation(
  child: PlaceCard(...),
  onTap: () {},
)
```

**Эффекты:**
- Микро масштабирование (0.98x)
- Увеличение тени
- Haptic feedback

### 5. Shimmer загрузка
```dart
MicroInteractions.shimmerLoading(
  isLoading: true,
  child: PlaceCard(...),
)
```

**Эффекты:**
- Градиентное мерцание
- Плавное движение
- Реалистичная загрузка

## 📱 Экраны приложения

### 1. Главный экран (Home)

**Структура:**
- AppBar с приветствием
- Поисковая строка
- Карусель избранных мест
- Категории (горизонтальный скролл)
- Сетка популярных мест

**Анимации:**
- Staggered появление элементов
- Автопрокрутка карусели
- Hover эффекты на карточках

### 2. Детальный экран места

**Структура:**
- Hero изображение с collapsing header
- Информационная карточка
- Описание места
- Ключевые факты
- Галерея изображений
- Интерактивная карта
- Отзывы

**Особенности:**
- Параллакс эффект
- Sticky header
- Плавающие кнопки действий

### 3. Экран избранного

**Структура:**
- Список избранных мест
- Свайп для удаления
- Пустое состояние с анимацией

**Анимации:**
- Staggered появление списка
- Свайп-анимация удаления
- Undo функциональность

### 4. Экран маршрутов

**Структура:**
- Список созданных маршрутов
- Прогресс-бары
- FAB для создания нового маршрута

## 🎯 Принципы анимации

### Длительность анимаций
```dart
animationFast: 150ms    // Быстрые микроинтерации
animationMedium: 300ms  // Стандартные переходы
animationSlow: 500ms    // Сложные анимации
```

### Кривые анимаций
```dart
easeInOut: Curves.easeInOut    // Стандартные переходы
easeOut: Curves.easeOut        // Быстрое начало
bounceOut: Curves.bounceOut    // Игривые эффекты
elasticOut: Curves.elasticOut  // Упругие анимации
```

### Haptic Feedback
```dart
HapticFeedback.lightImpact()  // Легкие касания
HapticFeedback.mediumImpact() // Средние нажатия
HapticFeedback.heavyImpact()  // Сильные действия
```

## 🌙 Темная тема

### Принципы
- Темный фон по умолчанию (#0F0F1F)
- Меньше контраста, но четкие разделения
- Градиенты более видимые на темном
- Светлые иконки (белый, светло-серый)

### Адаптация компонентов
```dart
// Проверка темы
bool isDark = DesignUtils.isDarkMode(context);

// Получение цветов
Color backgroundColor = DesignUtils.getBackgroundColor(context);
Color textColor = DesignUtils.getTextColor(context);
```

## ♿ Доступность

### Требования
- Минимум 18sp для основного текста
- Контрастность минимум 4.5:1
- Touch targets минимум 48dp
- Семантические лейблы для всех кнопок
- Поддержка screen reader

### Реализация
```dart
// Семантические лейблы
Semantics(
  label: 'Добавить в избранное',
  child: IconButton(...),
)

// Доступные размеры
Container(
  constraints: BoxConstraints(
    minWidth: 48,
    minHeight: 48,
  ),
  child: IconButton(...),
)
```

## 📦 Необходимые пакеты

```yaml
dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^4.0.0
  flutter_animate: ^4.2.0
  shimmer: ^3.0.0
  fl_chart: ^0.65.0
  cached_network_image: ^3.3.0
```

## 🚀 Быстрый старт

### 1. Импорт дизайн-системы
```dart
import 'package:travel_kz_app/design/design_system.dart';
import 'package:travel_kz_app/design/components/gradient_button.dart';
import 'package:travel_kz_app/design/components/glassmorphic_card.dart';
```

### 2. Использование темы
```dart
MaterialApp(
  theme: TravelKZDesign.lightTheme,
  darkTheme: TravelKZDesign.darkTheme,
  themeMode: ThemeMode.system,
  home: HomeScreen(),
)
```

### 3. Создание кнопки
```dart
GradientButton(
  text: 'Начать путешествие',
  onPressed: () {},
  gradient: TravelKZDesign.primaryGradient,
  icon: Icons.explore,
)
```

### 4. Создание карточки
```dart
PlaceCard(
  imageUrl: 'https://example.com/image.jpg',
  title: 'Алматы',
  location: 'Южный Казахстан',
  rating: 4.8,
  reviewCount: 1250,
  onTap: () {},
)
```

## 🎨 Экспорт дизайна

### JSON цветовой палитры
```json
{
  "primary": {
    "gradient": ["#667EEA", "#764BA2"],
    "description": "Основной градиент"
  },
  "accent": {
    "gradient": ["#F093FB", "#F5576C"],
    "description": "Акцентный градиент"
  },
  "secondary": {
    "gradient": ["#4FACFE", "#00F2FE"],
    "description": "Вторичный градиент"
  }
}
```

### Константы теней
```dart
// Уровни теней
shadowSmall: [BoxShadow(blurRadius: 3, offset: Offset(0, 1))]
shadowMedium: [BoxShadow(blurRadius: 6, offset: Offset(0, 2))]
shadowLarge: [BoxShadow(blurRadius: 12, offset: Offset(0, 4))]
shadowXLarge: [BoxShadow(blurRadius: 24, offset: Offset(0, 8))]
```

### Константы анимаций
```dart
// Длительность
animationFast: Duration(milliseconds: 150)
animationMedium: Duration(milliseconds: 300)
animationSlow: Duration(milliseconds: 500)

// Кривые
easeInOut: Curves.easeInOut
easeOut: Curves.easeOut
bounceOut: Curves.bounceOut
elasticOut: Curves.elasticOut
```

## 📱 Адаптивность

### Breakpoints
```dart
mobile: < 600dp
tablet: 600dp - 840dp
large: > 840dp
```

### Guidelines
- Все элементы работают на экранах 4.5" до 7"
- Padding: 16dp для мобиля, 24dp для планшета
- Размеры шрифтов увеличиваются на 2sp для планшета
- GridView: 2 колонки мобиль, 3+ колонки планшет

## 🎯 Лучшие практики

### 1. Использование анимаций
- Анимируйте только значимые изменения
- Используйте короткие анимации (150-300ms)
- Добавляйте haptic feedback для важных действий
- Тестируйте на реальных устройствах

### 2. Работа с цветами
- Всегда используйте DesignUtils для получения цветов
- Проверяйте контрастность в обеих темах
- Используйте градиенты умеренно
- Тестируйте на разных экранах

### 3. Создание компонентов
- Следуйте принципу единственной ответственности
- Добавляйте анимации по умолчанию
- Поддерживайте обе темы
- Документируйте параметры

### 4. Производительность
- Используйте const конструкторы где возможно
- Избегайте сложных анимаций в списках
- Кэшируйте изображения
- Оптимизируйте rebuilds

## 🔧 Настройка и кастомизация

### Создание собственного градиента
```dart
static const Gradient customGradient = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  colors: [Color(0xFF123456), Color(0xFF789ABC)],
);
```

### Создание собственной анимации
```dart
class CustomAnimation extends StatefulWidget {
  @override
  _CustomAnimationState createState() => _CustomAnimationState();
}

class _CustomAnimationState extends State<CustomAnimation>
    with SingleTickerProviderStateMixin {
  late AnimationController _controller;
  late Animation<double> _animation;

  @override
  void initState() {
    super.initState();
    _controller = AnimationController(
      duration: TravelKZDesign.animationMedium,
      vsync: this,
    );
    _animation = Tween<double>(begin: 0.0, end: 1.0)
        .animate(CurvedAnimation(
          parent: _controller,
          curve: TravelKZDesign.easeOut,
        ));
  }

  @override
  Widget build(BuildContext context) {
    return AnimatedBuilder(
      animation: _animation,
      builder: (context, child) {
        return Transform.scale(
          scale: _animation.value,
          child: child,
        );
      },
    );
  }
}
```

## 📊 Метрики и аналитика

### Отслеживаемые события
- Просмотр мест
- Добавление в избранное
- Создание маршрутов
- Использование поиска
- Переключение тем

### A/B тестирование
- Различные варианты кнопок
- Альтернативные анимации
- Разные цветовые схемы
- Варианты навигации

## 🎉 Заключение

TravelKZ представляет собой современную дизайн-систему, которая сочетает в себе:

- **Современные тренды** - Glassmorphism, градиенты, микроанимации
- **Казахстанскую айдентику** - цвета и элементы, отражающие культуру
- **Отличный UX** - интуитивные анимации и переходы
- **Высокую производительность** - оптимизированные компоненты
- **Доступность** - поддержка всех пользователей

Эта дизайн-система обеспечивает единообразный и премиальный пользовательский опыт во всем приложении, делая путешествия по Казахстану еще более увлекательными и запоминающимися.

---

*Создано с ❤️ для путешественников по Казахстану*
